// Client-side Sentry requires NEXT_PUBLIC_SENTRY_DSN (a public env var).
// Phase 1 only ships server-side Sentry via instrumentation.ts.
// See open question in Phase 1 acceptance notes for the client-side path.
